import { ColorFieldConfig } from "@/types";

export type ColorFieldProps = {
  field: ColorFieldConfig;
};

export default function ColorField({ field }: ColorFieldProps) {
  return <div>Color</div>;
}
