import {
  ArrayFieldConfig,
  TextFieldConfig,
  TextareaFieldConfig,
  SelectFieldConfig,
} from "@/types";

export default function FormBuilder() {
  return (
    <div>
      <ul>
        <li>
          <button type="button"></button>
        </li>
      </ul>
    </div>
  );
}
